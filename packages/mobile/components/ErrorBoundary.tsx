// Public re-export of the template-managed ErrorBoundary.
// Keeps the never-edit __ErrorBoundary implementation intact while
// exposing the import path the app conventions expect.
export { ErrorBoundary } from "./__ErrorBoundary";
